import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'w3w-sandbox-ang17',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
